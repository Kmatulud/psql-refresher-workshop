language: node_js
services:
  - postgresql
env:
  - DATABASE_URL
before_script:
  - psql -c 'create database garment_app' -U postgres
  - psql -U postgres -d garment_app -a -f ./sql/garment.sql
node_js:
  - 12